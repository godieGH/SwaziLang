# SwaziLang

A scripting language written in C++ that lets you program using natural Swahili keywords and syntax.

## build
``` bash
mkdir build
cd build
cmake ..
cmake --build .
```

## run
```bash
./swazi # In the build dir to use run an executable or add to path to run anywhere
```